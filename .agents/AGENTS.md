# Workspace Guidelines

## Codebase Conventions
- **ViewSets Location**: Always define ViewSets in a single `views.py` file per Django app (`apps/<app_name>/views.py`). Do not create nested `views/` directories.
- **Permissions Location**: Always define permissions in a single `permissions.py` file per Django app (`apps/<app_name>/permissions.py`).
